import type { Metadata } from "next";
import { buildPageMetadata } from "@/lib/seo/metadata";

export const metadata: Metadata = buildPageMetadata({
  title: "IT-решения",
  description: "Разработаем новый цифровой продукт под ваши задачи.",
  path: "/it-solutions",
});

export default function ItSolutionsPage() {
  return (
    <div>
      {/*
        TODO по ТЗ (IT-решения):
        - Hero: видео-фон, кнопка "Заказать услугу" → модалка "Оставить заявку"
        - "От точечных исправлений до глобальной цифровизации" — иконки-галочки 1..5
          последовательно сверху вниз меняют цвет серый→красный (~1.5s на элемент), остаются красными
        - "Результат, который можно измерить" — кнопка "Обсудить проект" → модалка "Расскажите о своём проекте"
        - "Этапы работы" — та же механика, что на /automation (пульсация точек таймлайна + градиентная обводка карточек)
      */}
    </div>
  );
}
