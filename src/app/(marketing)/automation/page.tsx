import type { Metadata } from "next";
import { buildPageMetadata } from "@/lib/seo/metadata";

export const metadata: Metadata = buildPageMetadata({
  title: "Автоматизация бизнес-процессов",
  description: "Масштабируйте бизнес и автоматизируйте процессы под ключ.",
  path: "/automation",
});

export default function AutomationPage() {
  return (
    <div>
      {/*
        TODO по ТЗ (Автоматизация):
        - Hero: видео-фон, кнопка "Заказать услугу" → модалка "Оставить заявку"
        - "Результаты автоматизации" — 4 карточки метрик, stagger fade-in снизу вверх (0.1s между карточками)
        - "До и После" — карточка "С автоматизацией": анимированный переливающийся градиент фона
        - "Этапы работы" — пульсирующие опорные точки таймлайна (CSS pulse);
          hover/tap на карточке → анимированная градиентная обводка по периметру + подсветка связанной точки
        - "Рассчитаем выгодно для вас" — кнопка "Получить расчет" → модалка "Расскажите о своём проекте"
      */}
    </div>
  );
}
